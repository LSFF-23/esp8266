#include <stdio.h>

int main(void) {
    int tarefas_completadas = 5;
    int total_tarefas = 8;
    float proporcao = (float)tarefas_completadas / (float) total_tarefas;

    // Seu código aqui
    printf("Proporção: %.2f\n", proporcao);
    return 0;
}
