#include <stdio.h>

// Seu código #define aqui
#define VELOCIDADE_MAXIMA 120

int main() {
    int velocidade_motor = VELOCIDADE_MAXIMA;
    printf("Velocidade máxima do motor: %d RPM\n", velocidade_motor);
    return 0;
}
